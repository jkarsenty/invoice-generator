[project]
name = "invoice-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["jinja2>=3.1.6", "weasyprint>=67.0"]

[project.scripts]
invoice-generator = "scripts.generate_invoice:main"
